node_modules/
dist/
*.log
.DS_Store
.vite/
.env
.env.local
.env.*.local

# Local announcement / promo copy — kept out of github by default
announce*.txt
promo*.txt

# Local planning / spec docs — not published
docs/plan.md
docs/superpowers/

# Runtime deployment config — holds the publish password; never commit
config.json
public/config.json
